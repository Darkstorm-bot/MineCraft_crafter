{
  "type": "object",
  "required": ["approval_flag", "delta_score", "iteration_count", "quality_score"],
  "properties": {
    "approval_flag": {"type": "boolean"},
    "delta_score": {"type": "number", "minimum": 0, "maximum": 100},
    "iteration_count": {"type": "integer", "minimum": 0},
    "quality_score": {"type": "number", "minimum": 0, "maximum": 100}
  }
}
